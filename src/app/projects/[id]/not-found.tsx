import { EmptyState } from "@/components/ui";

export default function ProjectNotFound() {
  return <EmptyState title="Project not found" description="It may have been removed or the link is wrong." />;
}

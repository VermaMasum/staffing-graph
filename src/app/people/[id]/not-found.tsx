import { EmptyState } from "@/components/ui";

export default function PersonNotFound() {
  return <EmptyState title="Person not found" description="It may have been removed or the link is wrong." />;
}
